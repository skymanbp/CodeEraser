{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "main window may invoke the report-family commands, the erase preview/apply pair, open the OS folder picker for the root field, and listen for ce-task progress events",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default", "dialog:allow-open"]
}
