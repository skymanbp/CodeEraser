{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "main window may invoke the two report commands",
  "windows": ["main"],
  "permissions": ["core:default"]
}
