# 计划 v1.2 delta 对抗性复审记录（Opus 攻击视角，第二轮）

> 2026-08-07 · 评审人：Claude Opus（attack perspective；Codex 额度耗尽期间的指定替代）
> 对象：docs/DEVELOPMENT_PLAN.md v1.2（12 项 grill 决策落地后）+ README/CLAUDE.md/LICENSE/.gitignore
> 方法：git diff v1.1→v1.2 逐 hunk 比对；官方 plugin-marketplaces 文档实证；git check-ignore 实测
> 净判决：v1.2 按当时状态**不可锁定**（2🔴+1🟡 必改）；处置后升 v1.3 锁定。

## 发现与处置

| # | 严重度 | 发现 | v1.3 处置 |
|---|---|---|---|
| D1-1 | 🔴 | `.gitignore:21` 的 `bin/` 匹配任意深度，静默吞掉 plugin/bin/ 启动脚本与 cli/src/bin/（git check-ignore 实测证实）；0.x 预览提前使其 M3 即爆 | 删除整个 Go 段（Go 已非实现语言），加注释说明 bin 必须提交；check-ignore 复测无匹配 |
| D2-1 | 🔴 | M4 评估集样本源自 0.x 预览 dogfood，但预览期 guard 已在塑形编辑分布——本该误报的编辑恰被 guard 劝阻，FPR 向下偏，deny 准入门自证（侵蚀 v1.0 评审 A5 的处置） | M3 增加**静默观察档**（只记录不拦截）；M4 验收加样本纯净度约束：只采观察档与 M3 前无 guard 会话，被干预编辑排除并报告比例 |
| D1-2 | 🟡 | memory/PLAN.md 仍是 v1.1 派生（含"License 待拍板"残留、M1 无 Go），计划头部"已由 cc-memory 锁定"对 v1.2 为假陈述 | 修订完成后重跑 ccm 锁定至 v1.3 |
| D2-2 | 🟡 | M3 新增 0.x 预览交付物但工期（2 周）与验收（零新增项）双双未动；plugin.json 无 version 与"发 0.x"矛盾 | M3 工期改 2–3 周 ±；验收加"干净环境安装成功 + dogfood ≥10 会话（观察档 ≥5）"；ADR-007 注明预览起带显式 version |
| D2-3 | 🟡 | 私有仓期间 GitHub Releases 无匿名下载，ADR-007"分发路径唯一化"在 M3–M6 实质走不通（marketplace 本身可行：本地路径/私有 git 均获官方支持，仅 auto-update 有已知坑） | §3 分发行注明预览期走 ADR-007 air-gapped 手动放置分支 |
| D2-4 | 🟡 | +Go 只在 M1 定价；M2 token 归一化、M5 graph/docdup 均逐语言，成本未反映 | M2 内容注明归一化覆盖五语言、工期 3 周 ±；M5 验收注明 import 边核对覆盖五语言 |
| D2-5 | 🟡 | docdup 注释域 × Apache-2.0 版权头交互：逐文件 license header 是天然重复源，排除模型无类别级豁免，会先在本仓库自炸 | §4.1 排除模型加类别级豁免：license 头、结构化 docstring 骨架不入 docdup 语料 |
| D1-3 | 🟡 | README 把 Codex/Kimi 并列为"插件"目标，超卖（计划只承诺 Claude Code 插件 + MCP/CI 集成） | README 收窄表述 |
| D1-4 | 🔵 | gui/ 目录在 README/.gitignore 存在、计划布局无 | 统一为 cli/（GUI 后端在 cli/ workspace） |
| D1-5 | 🔵 | `--roast` 与 codeeraser alias 只活在 §9 决策清单 | 下沉到 §3 CLI 行与 §4.1 |
| D1-6 | 🔵 | `# === Go (if used) ===` 残留"Go 作候选实现语言"旧含义 | 随 D1-1 一并删除 |
| D1-7 | 🔵 | 复用 MIT 代码（ast-grep-core）的署名义务无归属 | M7 加许可证合规（NOTICE/第三方清单） |
| D2-6 | 🔵 | gocognit"对拍"是唯一无量化门的验收 | 改为"分歧全部清单化归因，无未解释分歧" |
| D2-7 | 🔵 | 转公开时 git 历史带 memory/PROGRESS.md 等会话数据，无审计验收 | M7 验收加"转公开前全历史审计" |
| D2-8 | 🔵 | 私有仓三平台 CI 计费（macOS 10×）未评估 | M0 内容加额度评估（macOS 限 tag/夜间） |

无发现维度：决策传播完整性（正文零残留）、LICENSE 完整性、棘轮合规（等行改写零信息损失）、v1.0 九条缺陷无一被文字撤销（A5/A9a 两条实质侵蚀即 D2-1/D2-3，已处置）。
