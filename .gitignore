# === OS / editor ===
Thumbs.db
.DS_Store
*.swp
.vscode/
.idea/

# === Haskell (core/) ===
dist-newstyle/
.stack-work/
*.hi
*.o
cabal.project.local
cabal.project.local~

# === Rust (cli/) ===
# Cargo.lock is tracked for binaries — do NOT ignore it
# NOTE: no "bin/" rule here — plugin/bin/ launcher scripts and
# cli/src/bin/ MUST be committed (delta-review D1-1)
target/

# === Generated / scratch ===
*.log
*.tmp
scratch/
# raw hook captures carry machine paths; only sanitized *.golden.json are tracked
contracts/fixtures/hook-payloads/payloads.ndjson
