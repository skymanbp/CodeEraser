{
 "schema": "ce.eval-lang-review/1.0.0",
 "corpus": "stringr",
 "tip": "ae054b1d28f630fee22ddb3cb7525396e62af4fe",
 "lang": "r",
 "auditor": "four independent Opus subagents, one batch of 25 primaries each in the sample's audit order, reading only the two clones at their pinned tips and their batch - never the product's parse, no ce, no resolver; one auditor also confirmed its sites with R's own parser (R 4.6.1 parse + getParseData), as its notes record (booklet language-expansion.md section 14 item 14; RG15: assembled verbatim, judgments untouched)",
 "summary": {
  "external": 2
 },
 "rows": [
  {
   "rank": "00acb671d2f5c689acc136408d96d7f32a6b6b50fa47956dcf8260c1930e9e9e",
   "path": "R/view.R",
   "line": 198,
   "nth": 0,
   "kind": "library",
   "spec": "cli",
   "batch": 2,
   "truth": "external",
   "why": "Namespace-qualified call cli::is_utf8_output into the CRAN package cli, which stringr's DESCRIPTION lists under Imports; the corpus's only DESCRIPTION (at the root) declares Package: stringr and no cli directory exists, so cli is not defined here."
  },
  {
   "rank": "00c7cb01f6589b79c63e42227c328d6819e8edb7f767c06c160f6954f28ae154",
   "path": "R/c.R",
   "line": 59,
   "nth": 0,
   "kind": "library",
   "spec": "vctrs",
   "batch": 4,
   "truth": "external",
   "why": "`vctrs::vec_size_common` is a namespace-qualified call into the CRAN package vctrs, declared under Imports in stringr's DESCRIPTION; the corpus's only DESCRIPTION (at the root) declares Package: stringr, so vctrs is not defined here."
  }
 ],
 "site_gaps": [],
 "scope_gaps": [],
 "notes": [
  "All 25 rows are library-kind rows from covid19model; stringr contributed none to this batch. Package definition: the corpus holds exactly one DESCRIPTION file (git ls-files and find agree): covid19AgeModel/DESCRIPTION with Package: covid19AgeModel. No row's spec is covid19AgeModel, so every row is external. Site checks: every recorded line contains its spec; R 4.6.1's own parser (parse + getParseData over each whole file, all 22 files parse cleanly) finds exactly one package-reference token on each recorded line - a SYMBOL_PACKAGE followed by NS_GET (8 rows) or library(<SYMBOL>) (17 rows) - and it is the spec, so nth 0 is unambiguous and no site sits inside a string or comment. 'none' was excluded by a corpus-wide git grep: nothing rebinds library, require, requireNamespace, loadNamespace, suppressMessages or `::`/`:::`, and nothing assign()s those names. Rows in covid19AgeModel/R (the package's own source) still resolve pkg::f to the external namespace. The CRAN classification rests on the packages being well-known CRAN packages plus the project's own dependency declarations (DESCRIPTION Imports and the conda r-* environment files); parallel is the only base-R package in the batch. site_gaps lists three in-corpus loads seen in row files that no row of this batch covers; since the batch is a sample, the assembler should drop any the detector did report.",
  "Every row's spec text was confirmed on its recorded line, and each of those lines holds exactly one site, so nth=0 is consistent throughout. Package definitions: covid19model has exactly one DESCRIPTION (covid19AgeModel/DESCRIPTION, Package: covid19AgeModel) and stringr has exactly one (root DESCRIPTION, Package: stringr), checked with both git ls-files and a filesystem find; every other package named here is external (CRAN). No file in either corpus rebinds library/require/source/suppressMessages or the :: operator; the grep hits for 'source =' are named arguments (read_death_data(source = \"jhu\")) and data-frame columns (mutate(source = ...)), not rebindings. Working directory for covid19model source rows: repository root (top-level README run instructions, docker/README.md 'Rscript <filename>' in the mounted checkout, covid19model.Rproj at the root, no setwd/chdir in any tracked file; tiers/ scripts use here() for data paths, which also anchors at the project root). The covariate-effect-sizes.r row is the least directly evidenced, because no file in the repo sources that script; its truth rests on the project-wide convention plus the file's own root-relative output/input paths. Running it from its own directory would reach no in-corpus file, so 'dynamic' does not apply. Observed but out of scope (not listed as gaps): covid19AgeModel/inst/scripts/stan-make-data-usa-dev.r:6 system.file(package = \"covid19AgeModel\") only locates data and .stan files under the installed package (lines 57-75), loads no R code, and is not one of the listed library forms; covid19AgeModel/inst/R-utils/explore.r (e.g. :376) sources usa/code/utils/read-data-usa.r through a non-literal file.path(indir, ...), which falls outside the literal-string source definition; usa/code/plotting/covariate-effect-sizes.r:53 has 'bayesplot::get_color' inside a comment, which a detector must not report as a site.",
  "All 25 rows are covid19model rows; no stringr row is in this batch, so the stringr clone was not opened. Conventions: (1) every recorded line holds its spec and exactly one site, so each nth=0 is consistent. (2) A library row gets a corpus path only when a DESCRIPTION in the corpus declares that Package:; the corpus has exactly one, covid19AgeModel/DESCRIPTION, so every other package is external. (3) The working directory for the top-level base-*.r scripts is the repository root, per README.md (:37, :69, :83, :95, :105, :191), nature/README.md:4, the RStudio project at the root, and docker/Dockerfile (WORKDIR /var/model holding the repo contents). A git grep for setwd/chdir over every tracked file finds nothing, and nothing in the corpus rebinds library/require/source. (4) Row 354edd9b... (covid19AgeModel/inst/deprecated/R/foursquare_mobility_extend.R:4) names a file that does not exist anywhere in the corpus. I labelled it external (target not in this corpus) because no working directory reaches an in-corpus file; if the assembler keeps a separate class for a missing file, this is the only row that needs it. The function the script then calls, read_visitdata_mobility(), is defined with no arguments in usa/code/utils/read-data-usa-cases.r:137, which suggests a rename, but the literal path does not reach that file, so I did not answer it. Observed but not listed as gaps: covid19AgeModel/inst/make-analysis-covid19AgeModel-dev-HPC.R:4 system.file(package = \"covid19AgeModel\") references the in-corpus package without loading it; line 12 of the same file holds the string \"module load anaconda3/personal\\nsource activate covid19AgeModel\" (a shell command inside a string literal), which would be a false source site if a text-level detector reported it; Italy/code/utils/simulate-regional.r:133 and covid19AgeModel/inst/R-utils/explore.r source computed paths (paste0/file.path), outside the literal-string kinds.",
  "Both clones verified clean at their pinned commits (git status empty, HEAD = pinned sha). Package definitions: each corpus has exactly one DESCRIPTION file on disk and in git (stringr: ./DESCRIPTION, Package: stringr; covid19model: covid19AgeModel/DESCRIPTION, Package: covid19AgeModel), so every other package name is external. No file in either corpus rebinds library/require/requireNamespace/loadNamespace/source/sys.source (only function parameters named source inside read_death_data, not in any batch file), so no row is 'none'. Every spec occurs on its recorded line, and each line holds exactly one site, consistent with nth = 0. Working directory for covid19model's top-level scripts is the repository root: README.md:37/69/83/105/190-191, nature/README.md:4, docker/Dockerfile (WORKDIR holding the root-level files, CMD Rscript base.r base), docker/run_model_script.sh:12, .github/workflows/ci.yml:60, web-fetch-and-run.r:7 and the root covid19model.Rproj all agree, and no setwd/chdir exists anywhere outside covid19AgeModel/. Every source target is tracked in exact case and is the only tracked file whose path ends with the spec, so no alternative working directory reaches a different in-corpus file. Convention: a site inside if (FULL) (base-usa.r:183, and make-scenario-plots-top7.r, which is sourced inside if (FULL) at base-Italy.r:183) is answered with the file it reads when that branch runs, because the README says the code must be run in full mode; the branch decides whether the file is read, not which file. A nested source resolves against the caller's working directory because the outer source call passes no chdir. 'CRAN' in the whys names the provenance of well-known packages; the truth 'external' rests only on the package not being defined in the corpus."
 ]
}
